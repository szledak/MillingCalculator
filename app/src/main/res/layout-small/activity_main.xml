<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:fillViewport="true">
    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" android:background="#ffffff">

        <!--  Header  Starts-->
        <LinearLayout android:id="@+id/header"
            android:layout_width="fill_parent"
            android:layout_height="65dp"
            android:background="@drawable/header_gradient"
            android:paddingTop="5dip"
            android:paddingBottom="5dip">
            <!-- Logo Start-->

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textAppearance="?android:attr/textAppearanceLarge"
                android:id="@+id/textView"
                android:text="Milling Calculator"
                style="@style/logo_text" />
            <!-- Logo Ends -->
        </LinearLayout>
        <!--  Header Ends -->
        <!-- Footer Start -->
        <LinearLayout android:id="@+id/footer"
            android:layout_width="fill_parent"
            android:layout_height="30dp"
            android:background="@drawable/header_gradient"
            android:layout_alignParentBottom="true"
            android:orientation="horizontal">
        </LinearLayout>
        <!-- Footer Ends -->

        <!-- Login Form -->

        <Button
            style="@style/button_text"
            android:layout_width="fill_parent"
            android:text="@string/cutting_speed"
            android:id="@+id/btnCuttingSpeed"
            android:layout_gravity="center_horizontal"
            android:background="@color/result"
            android:layout_above="@+id/btnTableFeed"
            android:layout_alignLeft="@+id/btnTableFeed"
            android:layout_alignStart="@+id/btnTableFeed"
            android:layout_alignRight="@+id/btnTableFeed"
            android:layout_alignEnd="@+id/btnTableFeed" />

        <Button
            android:background="@color/result"
            style="@style/button_text"
            android:id="@+id/btnTableFeed"
            android:layout_width="fill_parent"
            android:text="@string/table_feed"
            android:layout_centerVertical="true"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:layout_marginTop="40dp"
            android:layout_marginBottom="40dp"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp" />

        <Button
            android:background="@color/result"
            style="@style/button_text"
            android:layout_width="fill_parent"
            android:text="@string/feed_fer_tooth"
            android:id="@+id/btnFeedPerTooth"
            android:layout_gravity="center_horizontal"
            android:layout_below="@+id/btnTableFeed"
            android:layout_alignLeft="@+id/btnTableFeed"
            android:layout_alignStart="@+id/btnTableFeed"
            android:layout_alignRight="@+id/btnTableFeed"
            android:layout_alignEnd="@+id/btnTableFeed" />
        <!-- Login Form Ends -->
    </RelativeLayout>
</ScrollView>